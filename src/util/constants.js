const PORT = 3000;
const DEFAULT = 'default';
const VALIDATION = 'Validation Errors';
const NOT_FOUND = 'Requested Resource Not Found';
const DUPLICATE = 'Resource already exists';

module.exports = {
    PORT,
    DEFAULT,
    VALIDATION,
    NOT_FOUND,
    DUPLICATE
}